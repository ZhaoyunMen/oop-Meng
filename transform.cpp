#include <iostream>
int transform(int number){

    Array[] = [];
    if number < 1{
        return 0
    }
    
    
     
    while number >= 1 {
        if number >1{
            if number % 2 != 0
                Array[].insert(0,1)
            if number %2 == 0
                Array[].insert(0,0)
        }
        if number == 1{
            Array[].insert(0,0)
            Array[].insert(0,1)
            number = number - 1
        }
    }
    return Array[]
    
}
