#include <iostream>
extern double* duplicateArray(double* array, int size);
int main(){
    double* newArray = new double[10];
    double* duplicateArray(double* newArray, int size);
    delete[] duplicateArray(newArray,size);
    return 0;
}