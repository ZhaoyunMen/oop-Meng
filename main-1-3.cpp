#include <iostream>
extern double* duplicateArray(double* array, int size);
int main(){
    double array[5] = {1,2,3,4,5};
    double* duplicateArray(array, int size);
    delete[] duplicateArray(newArray,size);
    return 0;
}